(function () {
  var interval = setInterval(function () {
    if (document.head) {
      // Check if <head> exists
      clearInterval(interval); // Stop checking once found
      var style = document.createElement("style");
      style.innerHTML = `.AB-EDC11 {
  /**
  * ----------------------------------------
          *			MODAL
  * ----------------------------------------
  */
}
.AB-EDC11 header {
  padding-top: 15px !important;
  padding-bottom: 25px !important;
}
.AB-EDC11 header a[href="/"] img {
  width: auto;
  height: 35px;
  object-fit: cover;
  margin-left: -45px;
}
.AB-EDC11
  .text-scheme-text.bg-scheme-background.pt-top-section-vertical-spacing.pb-bottom-section-vertical-spacing {
  padding-top: 75px;
}
.AB-EDC11
  .row-start-1.lg\:col-span-item.lg\:col-end-\[-1\]
  > .flex.flex-wrap.justify-end.items-top.gap-5 {
  flex-wrap: nowrap;
  gap: 15px;
}
.AB-EDC11
  .row-start-1.lg\:col-span-item.lg\:col-end-\[-1\]
  > .flex.flex-wrap.justify-end.items-top.gap-5
  a[href="/account/login"]
  + button {
  white-space: nowrap;
}
.AB-EDC11
  .row-start-1.lg\:col-span-item.lg\:col-end-\[-1\]
  > .flex.flex-wrap.justify-end.items-top.gap-5
  button {
  font-family: "PT Mono";
  font-weight: 400;
  font-size: 12px;
  line-height: 100%;
  letter-spacing: 0px;
  text-align: center;
  text-transform: uppercase;
  color: rgb(0, 0, 0);
}
.AB-EDC11__modal-layout {
  display: none;
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100vh;
  background: transparent;
  z-index: 99999;
  overflow: hidden;
  display: none;
  justify-content: center;
  align-items: flex-start;
  padding: 20px;
}
.AB-EDC11__modal-backdrop {
  display: none;
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.7);
}
.AB-EDC11__modal {
  opacity: 1;
  border: 1px solid rgb(0, 0, 0);
  background-color: rgb(232, 232, 232);
  border-radius: 5px;
  width: 100%;
  min-height: max-content;
  position: relative;
  z-index: 1;
  padding: 7px 5px 6px;
  overflow: hidden;
}
.AB-EDC11__modal__container {
  border: 1px solid rgb(0, 0, 0);
  border-radius: 4px;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}
.AB-EDC11__modal__top {
  border: 1px solid rgb(0, 0, 0);
  border-radius: 3px;
  margin: 6px 8px 23px;
  padding: 3px 7px;
  display: flex;
  align-items: center;
  justify-content: flex-start;
  height: 28px;
  max-height: 28px;
  overflow: hidden;
}
.AB-EDC11__modal__close-cta {
  margin: 0;
  margin-left: auto;
  padding: 5.22px;
  width: 22px;
  height: 22px;
  cursor: pointer;
  display: flex;
  justify-content: center;
  align-items: center;
}
.AB-EDC11__modal__close-cta svg {
  width: 11.5px;
  height: 11.5px;
}
.AB-EDC11__modal__search {
  display: flex;
  justify-content: flex-start;
  align-items: center;
  margin: 0;
  padding: 0;
  flex-grow: 1;
}
.AB-EDC11__modal__search-cta {
  width: max-content;
  padding: 2px;
  display: flex;
  justify-content: center;
  align-items: center;
  cursor: pointer;
}
.AB-EDC11__modal__search-cta svg {
  width: 100%;
  height: 12px;
  object-fit: contain;
}
.AB-EDC11__modal__search-input {
  width: 100%;
  padding: 4px 6px !important;
  border: none !important;
  outline: none !important;
  box-shadow: none !important;
  background: none !important;
  height: 100% !important;
  font-family: "PT Mono";
  font-weight: 400;
  font-size: 12px;
  line-height: 100%;
  letter-spacing: 0px;
  color: rgb(0, 0, 0);
  /* For Edge */
}
.AB-EDC11__modal__search-input:-webkit-autofill,
.AB-EDC11__modal__search-input:-webkit-autofill:hover,
.AB-EDC11__modal__search-input:-webkit-autofill:focus {
  -webkit-box-shadow: 0 0 0 1000px #ffffff inset !important; /* Change #ffffff to your desired background color */
  -webkit-text-fill-color: rgb(0, 0, 0) !important;
  background: none !important;
  box-shadow: none !important;
  -webkit-background-clip: text;
  background-clip: text;
}
.AB-EDC11__modal__search-input:hover,
.AB-EDC11__modal__search-input:focus,
.AB-EDC11__modal__search-input:active {
  border: none !important;
  outline: none !important;
  box-shadow: none !important;
  background: none !important;
}
.AB-EDC11__modal__search-input::placeholder {
  color: rgb(51, 51, 51);
}
.AB-EDC11__modal__search-input::-ms-input-placeholder {
  color: rgb(51, 51, 51);
}
.AB-EDC11__modal__featured-collections-container {
  margin: 0 15px 20px;
  display: flex;
  flex-direction: column;
  gap: 15px;
}
.AB-EDC11__modal__featured-collections-title {
  font-family: "PT Mono";
  font-weight: 400;
  font-size: 14px;
  line-height: 100%;
  letter-spacing: 0px;
  text-align: center;
  text-transform: uppercase;
  color: rgb(0, 0, 0);
}
.AB-EDC11__modal__featured-collections {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 15px;
}
.AB-EDC11__modal__featured-item {
  width: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 13.5px;
  border-radius: 3px;
  border: 1px solid rgb(0, 0, 0);
  outline: none;
  font-family: "PT Mono";
  font-weight: 400;
  font-size: 12px;
  line-height: 100%;
  letter-spacing: 0px;
  text-align: center;
  text-transform: uppercase;
  color: rgb(0, 0, 0);
}
.AB-EDC11__modal__featured-item:hover,
.AB-EDC11__modal__featured-item:active {
  border: 1px solid rgb(0, 0, 0);
  outline: none;
}
.AB-EDC11__modal__gear-drop-container {
  margin-bottom: 38px;
  display: flex;
  flex-direction: column;
  gap: 15px;
}
.AB-EDC11__modal__gear-drop-title {
  padding: 0 15px;
  font-family: "PT Mono";
  font-weight: 400;
  font-size: 14px;
  line-height: 100%;
  letter-spacing: 0px;
  text-align: center;
  text-transform: uppercase;
  color: rgb(0, 0, 0);
}
.AB-EDC11__modal__gear-drop-items {
  padding: 0 14px;
  display: flex;
  gap: 10px;
  justify-content: flex-start;
  overflow: scroll;
  /* Hide scrollbar for Chrome, Safari and Opera */
  /* Hide scrollbar for IE, Edge and Firefox */
  -ms-overflow-style: none;
  scrollbar-width: none;
}
.AB-EDC11__modal__gear-drop-items::-webkit-scrollbar {
  display: none;
}
.AB-EDC11__modal__gear-drop-item {
  width: 200px;
  min-width: 200px;
  display: flex;
  justify-content: flex-start;
  align-items: center;
  gap: 9px;
  padding: 5px 12px 5px 5px;
  border-radius: 5px;
  border: 1px solid rgb(0, 0, 0);
  outline: none;
}
.AB-EDC11__modal__gear-drop-item:hover,
.AB-EDC11__modal__gear-drop-item:active {
  border: 1px solid rgb(0, 0, 0);
  outline: none;
}
.AB-EDC11__modal__gear-drop-item-img {
  display: flex;
  justify-content: center;
  align-items: center;
}
.AB-EDC11__modal__gear-drop-item-img img {
  width: 60px;
  min-width: 60px;
  height: 60px;
  object-fit: cover;
  object-position: center;
  border-radius: 5px;
}
.AB-EDC11__modal__gear-drop-item-title {
  font-family: "PT Mono";
  font-weight: 400;
  font-size: 12px;
  line-height: 100%;
  letter-spacing: 0px;
  text-transform: capitalize;
  text-wrap: pretty;
  color: rgb(0, 0, 0);
  flex-grow: 1;
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.AB-EDC11--v2.AB-EDC11 .AB-EDC11__modal {
  padding: 6px 5px;
}
.AB-EDC11--v2.AB-EDC11 .AB-EDC11__modal__top {
  margin-top: 7px;
}
.AB-EDC11--v2.AB-EDC11 .AB-EDC11__modal__featured-collections-container {
  margin-bottom: 38px;
}
.AB-EDC11--v2.AB-EDC11 .AB-EDC11__modal__gear-drop-container {
  order: 1;
  margin-bottom: 22px;
}

.AB-EDC11--modal-show {
  overflow: hidden;
}
.AB-EDC11--modal-show .swell-tab {
  display: none !important;
}
.AB-EDC11--modal-show .AB-EDC11__modal-layout {
  display: flex;
}
.AB-EDC11--modal-show .AB-EDC11__modal-backdrop {
  display: block;
}
.slide-bottom {
  -webkit-animation: slide-bottom 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94) both;
  animation: slide-bottom 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94) both;
}

.slide-top {
  -webkit-animation: slide-top 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94) both;
  animation: slide-top 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94) both;
}

/**
  * ----------------------------------------
  * animation slide-bottom
  * ----------------------------------------
  */
@-webkit-keyframes slide-bottom {
  0% {
    opacity: 0;
    -webkit-transform: translateY(-200px);
    transform: translateY(-200px);
  }
  100% {
    opacity: 1;
    -webkit-transform: translateY(0);
    transform: translateY(0);
  }
}
@keyframes slide-bottom {
  0% {
    opacity: 0;
    -webkit-transform: translateY(-200px);
    transform: translateY(-200px);
  }
  100% {
    opacity: 1;
    -webkit-transform: translateY(0);
    transform: translateY(0);
  }
}
/**
  * ----------------------------------------
  * animation slide-top
  * ----------------------------------------
  */
@-webkit-keyframes slide-top {
  0% {
    opacity: 1;
    -webkit-transform: translateY(0);
    transform: translateY(0);
  }
  100% {
    opacity: 0;
    -webkit-transform: translateY(-200px);
    transform: translateY(-200px);
  }
}
@keyframes slide-top {
  0% {
    opacity: 1;
    -webkit-transform: translateY(0);
    transform: translateY(0);
  }
  100% {
    opacity: 0;
    -webkit-transform: translateY(-200px);
    transform: translateY(-200px);
  }
}
/**
  * ----------------------------------------
  * animation fade-in
  * ----------------------------------------
  */
@-webkit-keyframes fade-in {
  0% {
    opacity: 0;
  }
  50% {
    opacity: 0.5;
  }
  100% {
    opacity: 1;
  }
}
@keyframes fade-in {
  0% {
    opacity: 0;
  }
  50% {
    opacity: 0.5;
  }
  100% {
    opacity: 1;
  }
}
`;
      document.head.appendChild(style);
      setTimeout(() => {
        clearInterval(interval); // Clear the interval after 5 seconds
      }, 5000);
    }
  }, 100); // Check every 100ms for <head>
})();
const TEST_ID = "EDC11";
const VARIANT_ID = "V1"; /* V1, V2 */

function logInfo(message) {
  console.log(
    `%cAcadia%c${TEST_ID}-${VARIANT_ID}`,
    "color: white; background: rgb(0, 0, 57); font-weight: 700; padding: 2px 4px; border-radius: 2px;",
    "margin-left: 8px; color: white; background: rgb(0, 57, 57); font-weight: 700; padding: 2px 4px; border-radius: 2px;",
    message,
  );
}

logInfo("fired");

(async () => {
  const TEST_CONFIG = {
    client: "Acadia",
    project: "urbanedc",
    host: "https://urbanedc.com",
    test_name:
      "EDC11: [HEADER - Mobile] Add Search Into Header with Panel - (2) SET UP TEST",
    page_initials: "AB-EDC11",
    test_variation: 1 /* 1, 2 */,
    test_version: 0.0001,
  };

  const { host, page_initials, test_variation, test_version } = TEST_CONFIG;

  const ASSETS = {
    cross_svg: /* HTML */ `<svg
      width="12"
      height="12"
      viewBox="0 0 12 12"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
    >
      <path
        d="M0.649 11.5647L0 10.9157L5.13333 5.78233L0 0.649L0.649 0L5.78233 5.13333L10.9157 0L11.5647 0.649L6.43133 5.78233L11.5647 10.9157L10.9157 11.5647L5.78233 6.43133L0.649 11.5647Z"
        fill="black"
      />
    </svg> `,
    search_svg: /* HTML */ `<svg
      width="13"
      height="13"
      viewBox="0 0 13 13"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
    >
      <path
        d="M12.5001 12.5001L9.6048 9.6048M9.6048 9.6048C10.1001 9.10955 10.4929 8.52159 10.761 7.8745C11.029 7.22742 11.1669 6.53387 11.1669 5.83347C11.1669 5.13307 11.029 4.43953 10.761 3.79244C10.4929 3.14535 10.1001 2.5574 9.6048 2.06214C9.10955 1.56688 8.52159 1.17402 7.8745 0.905986C7.22742 0.637954 6.53387 0.5 5.83347 0.5C5.13307 0.5 4.43953 0.637954 3.79244 0.905986C3.14535 1.17402 2.5574 1.56688 2.06214 2.06214C1.06192 3.06236 0.5 4.41895 0.5 5.83347C0.5 7.248 1.06192 8.60458 2.06214 9.6048C3.06236 10.605 4.41895 11.1669 5.83347 11.1669C7.248 11.1669 8.60458 10.605 9.6048 9.6048Z"
        stroke="black"
        stroke-linecap="round"
        stroke-linejoin="round"
      />
    </svg> `,
  };

  const DATA = {
    featured_collections: [
      {
        title: "Knife",
        link: "https://urbanedc.com/collections/in-stock-knives",
      },
      {
        title: "Patch",
        link: "https://urbanedc.com/collections/urban-edc-ranger-eye-patches",
      },
      {
        title: "Fidget",
        link: "https://urbanedc.com/collections/in-stock-games",
      },
      {
        title: "Mutitool",
        link: "https://urbanedc.com/collections/in-stock-keychains-multitools",
      },
      {
        title: "Flashlight",
        link: "https://urbanedc.com/collections/in-stock-flashlights",
      },
      {
        title: "Lanyard",
        link: "https://urbanedc.com/collections/in-stock-lanyard-beads",
      },
    ],
  };

  function fireGA4Event(eventName, eventLabel = "") {
    console.log("GA4 Event Fired:", eventName, eventLabel);
    window.dataLayer = window.dataLayer || [];
    window.dataLayer.push({
      event: "GA4event",
      "ga4-event-name": "cro_event",
      "ga4-event-p1-name": "event_category",
      "ga4-event-p1-value": eventName,
      "ga4-event-p2-name": "event_label",
      "ga4-event-p2-value": eventLabel,
    });
  }

  async function fetchAndParseURLApi(url) {
    try {
      const response = await fetch(url);
      if (!response.ok) throw new Error(`HTTP ${response.status}`);

      const html = await response.text();

      const dom = new DOMParser().parseFromString(html, "text/html");

      return dom;
    } catch (error) {
      // console.error("Fetch and parse failed:", error);
      return null;
    }
  }

  async function getGearDropData() {
    const doc = await fetchAndParseURLApi(host);
    const targetSection = qq(doc, "section").find((section) =>
      qq(section, "h2.font-heading.text-heading-standard.break-words").some(
        (h2) => h2.innerText.includes("This Week's Gear Drop:"),
      ),
    );

    return qq(
      targetSection,
      "ul.carousel-track.flex.overflow-x-scroll.scroll-smooth.gap-media-grid.items-start.snap-x.snap-mandatory.snap-always.relative li",
    ).map((item) => ({
      title: q(item, ".break-words")?.innerText?.trim(),
      link: q(item, "a").getAttribute("href"),
      imgUrl: q(item, "img.tile-media").getAttribute("src"),
    }));
  }

  async function waitForElementAsync(
    predicate,
    timeout = 20000,
    frequency = 150,
  ) {
    const startTime = Date.now();

    return new Promise((resolve, reject) => {
      if (typeof predicate === "function" && predicate()) {
        return resolve(true);
      }

      const interval = setInterval(() => {
        const elapsed = Date.now() - startTime;

        if (elapsed >= timeout) {
          clearInterval(interval);
          return reject(
            new Error(
              `Timeout of ${timeout}ms reached while waiting for condition: ${predicate.toString()}`,
            ),
          );
        }

        if (typeof predicate === "function" && predicate()) {
          clearInterval(interval);
          return resolve(true);
        }
      }, frequency);
    });
  }

  function q(s, o) {
    return o ? s.querySelector(o) : document.querySelector(s);
  }

  function qq(s, o) {
    return o ? [...s.querySelectorAll(o)] : [...document.querySelectorAll(s)];
  }

  function animate(targetElement, className, interval) {
    if (!targetElement) return;
    if (className.includes(".")) className.replace(".", "");
    targetElement.classList.add(className);
    setTimeout(() => targetElement.classList.remove(className), interval);
  }

  function preventScroll(e) {
    if (
      (e.target === document.body || document.body.contains(e.target)) &&
      !e.target.closest(".AB-EDC11__modal__gear-drop-items")
    ) {
      e.preventDefault();
    }
  }

  function handleModalView(action = "show") {
    const modalShowClass = `${page_initials}--modal-show`;
    const body = document.body;

    const modal = q(`.${page_initials}__modal`);
    if (action === "show" && !body.classList.contains(modalShowClass)) {
      animate(modal, "slide-bottom", 200);
      modal.classList.add("slide-bottom");
      body.classList.add(modalShowClass);
      document.addEventListener("touchmove", preventScroll, { passive: false });
    }

    if (action === "hide") {
      animate(modal, "slide-top", 200);
      setTimeout(() => body.classList.remove(modalShowClass), 200);
      document.removeEventListener("touchmove", preventScroll);
    }
  }

  async function createComponent() {
    const gear_drop_data = await getGearDropData();

    // Search Open Cta
    q(
      ".row-start-1.lg\\:col-span-item.lg\\:col-end-\\[-1\\] > .flex.flex-wrap.justify-end.items-top.gap-5",
    ).insertAdjacentHTML(
      "afterbegin",
      `<button class="${page_initials}__modal__show-cta uppercase js-enabled block lg:hidden" type="button">Search</button>`,
    );

    // Modal
    q("body").insertAdjacentHTML(
      "afterbegin",
      /* HTML */ `
        <div class="${page_initials}__modal-layout">
          <div class="${page_initials}__modal-backdrop"></div>
          <div class="${page_initials}__modal">
            <div class="${page_initials}__modal__container">
              <div class="${page_initials}__modal__top">
                <div class="${page_initials}__modal__search">
                  <button
                    class="${page_initials}__modal__search-cta"
                    type="button"
                  >
                    ${ASSETS.search_svg}
                  </button>
                  <input
                    id="ab-search"
                    type="text"
                    class="${page_initials}__modal__search-input"
                    placeholder="Search"
                    autocomplete="off"
                  />
                </div>
                <button
                  class="${page_initials}__modal__close-cta"
                  type="button"
                >
                  ${ASSETS.cross_svg}
                </button>
              </div>
              <div class="${page_initials}__modal__gear-drop-container">
                <div class="${page_initials}__modal__gear-drop-title">
                  This Week's Gear Drop
                </div>
                <div class="${page_initials}__modal__gear-drop-items">
                  ${gear_drop_data
                    .map(
                      ({ title, link, imgUrl }) => /* HTML */ `
                        <a
                          href="${host + link}"
                          class="${page_initials}__modal__gear-drop-item"
                        >
                          <span
                            class="${page_initials}__modal__gear-drop-item-img"
                            ><img src="${imgUrl}" alt="${title}"
                          /></span>
                          <span
                            class="${page_initials}__modal__gear-drop-item-title"
                            >${title}</span
                          >
                        </a>
                      `,
                    )
                    .join("")}
                </div>
              </div>
              <div
                class="${page_initials}__modal__featured-collections-container"
              >
                <div
                  class="${page_initials}__modal__featured-collections-title"
                >
                  Featured Collections
                </div>
                <div class="${page_initials}__modal__featured-collections">
                  ${DATA.featured_collections
                    .map(
                      ({ title, link }) =>
                        `<a href="${link}" class="${page_initials}__modal__featured-item"> ${title} </a>`,
                    )
                    .join("")}
                </div>
              </div>
            </div>
          </div>
        </div>
      `,
    );

    // Event Listeners
    q(`.${page_initials}__modal__show-cta`).addEventListener("click", () =>
      handleModalView("show"),
    );
    q(`.${page_initials}__modal__close-cta`).addEventListener("click", () =>
      handleModalView("hide"),
    );
    q(`.${page_initials}__modal-backdrop`).addEventListener("click", (e) => {
      if (!e.target.closest(`.${page_initials}__modal`)) {
        handleModalView("hide");
      }
    });
    q(`.${page_initials}__modal__search-cta`).addEventListener("click", () => {
      fireGA4Event("EDC11_ClickedSearch");
      q(
        '#DrawerMenu form[action="/search"] button[aria-label="Search"]',
      ).click();
    });
    q(`.${page_initials}__modal__search-input`).addEventListener(
      "input",
      (e) => {
        qq(
          '#DrawerMenu form[action="/search"] input[type="search"][name="q"]',
        ).forEach((item) => (item.value = e.target.value));
      },
    );
    qq(`.${page_initials}__modal__featured-item`).forEach((item) => {
      item.addEventListener("click", (e) => {
        e.preventDefault();
        fireGA4Event("EDC11_ClickedCategory", e.target.innerText);
        setTimeout(
          () => (window.location.href = e.target.getAttribute("href")),
          150,
        );
      });
    });
  }

  function init() {
    q("body").classList.add(
      page_initials,
      `${page_initials}--v${test_variation}`,
      `${page_initials}--version:${test_version}`,
    );
    console.table(TEST_CONFIG);
    createComponent();
  }

  function checkForItems() {
    return !!(
      q(
        `body:not(.${page_initials}):not(${page_initials}--v${test_variation})`,
      ) &&
      q(".row-start-1.lg\\:col-span-item.lg\\:col-end-\\[-1\\]") &&
      q('#DrawerMenu form[action="/search"] button[aria-label="Search"]')
    );
  }

  try {
    await waitForElementAsync(checkForItems);
    init();
  } catch (error) {
    console.warn(error);
    return false;
  }
})();
